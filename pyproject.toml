[project]
name = "tire-system-calculator"
version = "0.1.0"
description = "A Python tire-system speed calculator for GitHub Pages"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "pandas>=2.3.3",
]

[dependency-groups]
dev = [
    "playwright>=1.61.0",
]
